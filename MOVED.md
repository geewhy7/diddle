# ⚠️ MOVED

This project now lives in **/home/pi/py/clownbot/diddle** (with full git history).
This directory is an inert backup — the systemd service diddle-backend was
replaced by clownbot-backend running from the new location.
